# Not finish yet
def search_names(logins):
    login_ = []
    for i in range(len(logins)):
        if '_' in logins[i][0]:
            login_.append(logins[i])
    

    return login_
a = [[ "foobar_", "foo@foo.com" ], [ "bar_", "bar@bar.com" ] ]
filter(search_name, '_')
print(b)
        
